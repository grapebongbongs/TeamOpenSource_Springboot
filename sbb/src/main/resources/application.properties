# -------------------------------
# ✅ 프로젝트 기본 설정
# -------------------------------
spring.application.name=sbb
server.port=8080

# -------------------------------
# ✅ MySQL 데이터베이스 설정
# -------------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/menhaera?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=menhaerauser
spring.datasource.password=qhdfkrzmfhtm
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# -------------------------------
# ✅ JPA (Hibernate) 설정
# -------------------------------
spring.jpa.hibernate.ddl-auto=update     
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# -------------------------------
# ✅ Thymeleaf 설정
# -------------------------------
spring.thymeleaf.cache=false

# -------------------------------
# ✅ JSON / 시간대 설정
# -------------------------------
spring.jackson.time-zone=Asia/Seoul

# -------------------------------
# ✅ Push 알림 (VAPID)
# -------------------------------
#                  app.vapid.public-key=${VAPID_PUBLIC_KEY:}
#                  app.vapid.private-key=${VAPID_PRIVATE_KEY:}
#                  app.push.subject=mailto:admin@example.com

# -------------------------------
# ✅ Scheduler (매일 오전 9시 실행)
# -------------------------------
#                  app.schedule.notify-cron=0 0 9 * * *

logging.level.org.springframework.security=DEBUG
